#include<stdio.h>
int main()
{
   float r,A;
   printf("Enter the radius of circule: ");
   scanf("%f",&r);
   A=3.14*r*r;
   printf("Area of circle is %f and having the radius %f",A,r);
   return 0;
}
